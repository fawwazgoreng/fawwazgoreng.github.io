---
import BehaviorCardAstroComponent from "../ui/BehaviorCard.astro";
import { getEntry , getCollection } from "astro:content";

export type BehaviorCardType = {
    type: string;
    title1: string;
    title2: string;
    description: string;
    reason: string;
};

const allEntries = await getCollection("behavior");
console.log("Daftar file di koleksi behavior:", allEntries);

const entry = await getEntry("behavior", "data");
console.log("Hasil getEntry:", entry);

// const entry = await getEntry("behavior" , "data");

// if (!entry) {
//     throw new Error("Behavior data not found");
// }

// const data: BehaviorCardType[] = entry.data;
---

<!-- {
    data.map((item, i) => {
        return (
            <div class="mx-auto max-w-5xl px-6 py-8 md:py-10">
                <BehaviorCardAstroComponent {...item} index={i} />
            </div>
        );
    })
} -->
